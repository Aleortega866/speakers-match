# Neon (neon.tech) + Vercel
# En Neon: Dashboard → Connection Details → pestaña "Prisma"
#   DATABASE_URL = URL "Pooled" (para la app en Vercel)
#   DIRECT_URL   = URL "Direct" (solo para migraciones con prisma migrate)

DATABASE_URL="postgresql://user:pass@ep-xxx.pooler.us-east-1.aws.neon.tech/neondb?sslmode=verify-full"
DIRECT_URL="postgresql://user:pass@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=verify-full"

# Zapier webhooks (outreach va por Mailchimp API; mantener solo los que sigan en Zap)
ZAPIER_WEBHOOK_FORM_STARTED=""    # Zap: form iniciado
ZAPIER_WEBHOOK_FORM_COMPLETED=""  # Zap: form completado

# Mailchimp Marketing API — campaña outreach desde /admin/importar
MAILCHIMP_API_KEY=""
MAILCHIMP_AUDIENCE_ID=""       # list_id del Audience
MAILCHIMP_SERVER_PREFIX=""   # ej. us1 (ver URL de la cuenta en Mailchimp)
MAILCHIMP_FROM_EMAIL=""      # remitente verificado en Mailchimp
MAILCHIMP_FROM_NAME=""
MAILCHIMP_TEMPLATE_ID=""     # ID numérico del template en Mailchimp
# Opcional — asunto del correo (default: Invitación SpeakerMatch)
# MAILCHIMP_SUBJECT_LINE=""

# URL pública del sitio (enlaces de invitación /start?t= en admin). Sin barra final.
# NEXT_PUBLIC_APP_URL="https://tudominio.com"

# Calendly
NEXT_PUBLIC_CALENDLY_URL=""
CALENDLY_WEBHOOK_SECRET=""   # Protege /api/calendly-booked (Zapier lo manda como ?secret=)

# Opcional: seed de usuario admin local
ADMIN_SEED_EMAIL=""
ADMIN_SEED_PASSWORD=""
